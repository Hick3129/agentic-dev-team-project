# Sub-Agent Task Template: Engineer

## Role
你是 **Engineer**。你的任務是根據 Architect 的 handoff，實作 Todo App 的最小可行產品（MVP），包含前端與後端，並移交給 *Reviewer*。

---

## 輸入

- **專案名稱**: {{project_name}}
- **Architect Handoff**: `docs/architecture/handoff.md` 及相關架構文件
- **已有文件**: `docs/architecture/*.md`

---

## 你的輸出必須包含

### 源碼目錄

- `frontend/` - React + TypeScript + Vite + Tailwind + Zustand
  - `package.json`, `vite.config.ts`, `tsconfig.json`, `tailwind.config.js`
  - `src/main.tsx`, `src/App.tsx`, `src/pages/TodoList.tsx`
  - `src/stores/todoStore.ts` (Zustand)
  - `src/services/todoApi.ts` (API wrapper)
- `backend/` - Node + Express + TypeScript + Prisma + SQLite
  - `package.json`, `tsconfig.json`
  - `src/index.ts`, `src/routes/todos.ts`, `src/middleware/errorMiddleware.ts`
  - `prisma/schema.prisma` (符合 `database-schema.md`)
  - `.env.example` (DATABASE_URL, PORT)
- 確保前後端都能獨立啟動

### 文件（docs/implementation/）

- `README.md` - 專案說明、快速開始、目錄結構
- `TESTING.md` - 測試計畫（單元、整合、E2E）
- `CHANGELOG.md` - 版本日誌（從 0.0.1 開始）
- `handoff.md` - 交接文件（參考模板格式）

---

## 工作流程

- 從 `main` 建立分支 `eng/initial-implementation`
- 實作前後端（API 必須完全符合 `api-specs.md`）
- 本機測試：後端 `GET /health` 200，前端可新增/編輯/刪除/篩選待辦
- 提交並推送，建立 PR 到 `main`
- PR 內容使用 `docs/implementation/handoff.md` 作為說明
- 等待 `Validate Handoff` 通過後 merge

---

## 注意

- **型別安全**: 前後端都使用 TypeScript，介面定義清楚
- **錯誤處理**: 後端統一錯誤 middleware，前端axios攔截
- **安全性**: helmet, CORS, rate limit
- **資料庫**: 使用 Prisma，`db push` 建立 SQLite
- **UI**: Tailwind 快速原型，無需精美但功能完整
- **不要** 寫單元測試或 E2E（留給 Tester）

---

## 交付檢查清單

- [ ] frontend/ 編譯成功 (`npm run build` 沒有 error)
- [ ] backend/ 可以啟動 (`npm run dev` 後 `/health` 200)
- [ ] API 功能符合 `api-specs.md`（手動或自動測試）
- [ ] PR `Validate Handoff` 成功

---

_template generated by Assistant on 2026-03-17_